# Platform shims: file associations, OS dark-mode, native file picker. [SDS §13]
# ponytail: stub — per-platform impl at M1+

add_library(shell-platform STATIC
    # ponytail: stub
)
target_link_libraries(shell-platform PUBLIC Qt6::Widgets)
