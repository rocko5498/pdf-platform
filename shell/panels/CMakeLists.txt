# Side panels: outline, thumbnails, annotations, search results. [SDS §13, PRD]
# ponytail: stub — M1+ implementation

add_library(shell-panels STATIC
    # ponytail: stub
)
target_link_libraries(shell-panels PUBLIC shell-bridge Qt6::Widgets)
