# Conventions

- Specs ground truth; cite FR-*/ADR-*/SDS § in commits
- Hard rules GR-1..10 (AGENTS.md / IG §3)
- Code nav: Serena (languages: rust) → not Read on .rs when LS works
- `unsafe` needs SAFETY comment + review (ADR-027)
- Security paths (sandbox/redaction/sig/FFI) human-gated drafts
- Small commits; tests with change
- PDF fixtures: binary via .gitattributes (xref offsets)
