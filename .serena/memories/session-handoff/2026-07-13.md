# Session Handoff — 2026-07-13

## Status: Clean build, 102 tests, 0 warnings

## What was implemented this session

### 1. Windows Named Pipe IPC (ADR-031 compliance)
- Replaced TCP loopback with named pipes in sandbox/src/transport.rs
- Raw FFI: CreateNamedPipeW, ConnectNamedPipe, CreateFileW, ReadFile, WriteFile, CancelSynchronousIo
- Thread-based recv_with_timeout using CancelSynchronousIo for timeout support
- Updated spawn.rs: ENV_IPC_PORT -> ENV_IPC_PIPE

### 2. Command Routing Layer
- Typed Command enum (RenderTile, Inspect, Quit) with CMD:<TYPE>:<correlation_id> wire format
- Typed WorkerEvent enum (TileReady, RenderError, Summary) with EVT:<TYPE>:<correlation_id> wire codec
- Backward-compatible with legacy raw-byte commands
- Correlation IDs for request-response matching
- Updated worker dispatch, coordinator session, coordinator render loop, ffi-bridge

### 3. Tile Rendering Pipeline Integration
- Added col/row fields to Command::RenderTile and TileSlotDesc
- Worker passes grid position through to response (multi-tile correlation works)
- FFI bridge uses typed commands/events
- Worker death detection + respawn in RenderLoop (handle_worker_death())

### 4. Grid Layout System
- PageGeometry (per-page dimensions with rotation)
- PageLayout enum (Single, Continuous, Facing, ContinuousFacing)
- ViewportState (scroll, zoom, layout, velocity, page gap, facing gutter)
- PagePositioner (page_y_offset, page_x_offset, compute_visible_regions, build_viewport)
- Scale bucketing (14 standard zoom levels)
- Velocity-aware prefetch margin (2-8 tiles based on scroll velocity)

### 5. Viewport-to-RenderPipeline Wiring
- RenderLoop now owns PagePositioner + ViewportState
- update_viewport_state() — single-call API: state → viewport → schedule → dispatch → cache
- get_visible_tiles() — returns cached tiles visible in current viewport
- current_page(), page_count() accessors

### 6. Zoom Support
- zoom_by(factor, focus_x, focus_y) — zoom toward pointer
- zoom_by_center(factor) — zoom toward viewport center
- zoom_to(scale), zoom_to_fit_width/height/page, zoom_to_rect
- zoom_in_step(), zoom_out_step() — standard zoom levels
- zoom_to_actual_size() — reset to 100%
- min_scale/max_scale bounds

### 7. Keyboard Shortcuts & Mouse Drag Panning
- ToolMode enum (Select, Hand, ZoomToRect, Measure, Ink, Shape, Stamp, Note, Redact)
- InputAction enum (25+ actions: scroll, zoom, layout, tools, view controls)
- InputEvent enum (KeyDown, MousePress, MouseMove, MouseRelease, Wheel, Pinch)
- ShortcutRegistry with 30+ default shortcuts matching Acrobat conventions
- ViewController: process_event() maps events to actions based on tool mode
- DragState for hand-tool panning and middle-click pan

## Workspace state
- 0 errors, 0 warnings across all crates
- 102 tests passing across 54 test suites
- All tasks (T1-T19) completed

## Files modified this session
- core/sandbox/src/transport.rs — Named pipe transport (Windows)
- core/sandbox/src/spawn.rs — Named pipe spawn (Windows)
- core/sandbox/Cargo.toml — Removed windows-sys dep
- core/protocol/src/commands.rs — Command enum, typed+legacy codec, col/row
- core/protocol/src/events.rs — WorkerEvent enum with wire codec
- core/protocol/src/handles.rs — PixelFormat::from_u32()
- core/coordinator/src/render.rs — Typed commands, worker death, ViewportState integration
- core/coordinator/src/session.rs — Typed inspect, correlation IDs
- core/worker-main/src/main.rs — Typed dispatch, col/row passthrough
- core/ffi-bridge/src/lib.rs — Typed commands/events, correlation IDs
- core/render-pipeline/src/lib.rs — Added layout, input modules
- core/render-pipeline/src/layout.rs — PageLayout, ViewportState, PagePositioner, scale bucketing, zoom
- core/render-pipeline/src/input.rs — ToolMode, InputAction, ShortcutRegistry, ViewController
- core/render-pipeline/src/scheduler.rs — prefetch_margin parameter
- core/worker-main/tests/*.rs — Updated to typed protocol
