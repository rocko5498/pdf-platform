# PDFium Provenance

**Origin:** https://pdfium.googlesource.com/pdfium  
**License:** BSD 3-Clause (see LICENSE file in upstream source)  
**Snapshot:** TBD — fill in commit hash and date when prebuilt is pinned  

## Prebuilt binaries

Prebuilt static libraries live in `prebuilt/` and are checked in for hermetic
CI builds. Each binary must be reproduced from the pinned commit above.

## Patches

Local patches in `patches/` are applied in order. Each patch file is named
`NNNN-description.patch` and documents its motivation in the header.

ponytail: stub — populate when PDFium prebuilt is pinned at M0.
