[package]
name        = "diagnostics"
description = "Tracing setup, privacy-redaction wrapper types. [ADR-020, ADR-025]"
version.workspace = true
edition.workspace = true

license.workspace     = true
[dependencies]
tracing            = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
