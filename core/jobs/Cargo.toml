[package]
name        = "jobs"
description = "Declarative job DAG scheduler, priority classes, cancel tokens. STUB — M6. [ADR-009]"
version.workspace = true
edition.workspace = true
license.workspace = true
[dependencies]
protocol = { path = "../protocol" }
