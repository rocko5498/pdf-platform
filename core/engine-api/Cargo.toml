[package]
name        = "engine-api"
description = "Engine capability traits. All engine calls go through here. [ADR-005, ADR-025]"
version.workspace = true
edition.workspace = true

[dependencies]
pdf-types = { path = "../pdf-types" }
