[package]
name        = "sign"
description = "PAdES signing + validation. STUB — M8. [SDS §2.8, ADR-016]"
version.workspace = true
edition.workspace = true

[dependencies]
pdf-types = { path = "../pdf-types" }
pdf-cos   = { path = "../pdf-cos" }
pdf-write = { path = "../pdf-write" }
