[package]
name        = "search"
description = "In-doc find + opt-in local Tantivy cross-doc index. STUB — M2. [ADR-019]"
version.workspace = true
edition.workspace = true
license.workspace = true
[dependencies]
pdf-types    = { path = "../pdf-types" }
text-extract = { path = "../text-extract" }
engine-api   = { path = "../engine-api" }
# MIT. Tier-1 per ADR-028; adoption note: docs/adr/0034-tantivy-adoption.md.
tantivy      = "0.22"
