[package]
name        = "protocol"
description = "Typed command/event/handle-descriptor protocol. Shell, CLI, workers are peer clients. [ADR-004, ADR-031]"
version.workspace = true
edition.workspace = true

license.workspace     = true
[dependencies]
pdf-types = { path = "../pdf-types" }
